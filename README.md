# Week 3 SQL Assignment

**Name:** Muhammad Ahmad

**Tool Used:** Microsoft SQL Server Management Studio (SSMS)

## Repository Contents

- section1_queries.sql
- section2_3_queries.sql

## Setup Challenge

One challenge was converting the MySQL schema provided in the assignment into SQL Server syntax. I replaced MySQL-specific commands such as `DROP TABLE IF EXISTS` and `LIMIT` with SQL Server equivalents so the queries could run successfully in SSMS.
